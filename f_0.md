# What is function?

$$ f(x)$$

## What is this two strange alphabet?

Obviously, it is a f and a bracked x. But why "f"? And, why is thr "f" look doesn't "f"?

"f" at here stand for "function". And it looks like a "s" with line because mathematician want to different this "f" with another alphabets. It is not the first time mathematician do this. For example :
$$ x $$
This x is does not same like our "x" in daily life. We often use this "x", but why mathematicians don't use?

Because this "x" is to simmilar as "×"(the simbol of multiplu),"ⅹ"(a special x at Unicode) and "χ"(Greeze alphabet)

Now, we know why "f" and "x". Of course we can change this two to other alphabet, and we often do this. But f(x) is still the most popular function.

## The definition of function

What is the "function" thing means for? Function is a relation between the "f" and "x". For example:
$$ f(x)= x+2$$
In this function, we know that f is x+2. If we put 2 into the bracket, it will show:
$$ f(2)=4$$
From this example, we know that how to use function with simple arithmetic. We could also put other relation in this function.
Such as :
$$f(x)=sin(x),f(30)=sin30=0.5$$
$$f(x)=cos(x),f(30)=cos30=\frac {\sqrt[2]{3}}{2}$$
With this function, we can perform the relation.

## Combination with "="

We used to use "=" to perform relation between variable or number. For example:
$$y=x^2$$
After learning function, we can use function to perform the same relation.
$$f(x)=x^2$$
$$y=f(x)$$
You may think this is not nessesary and waste your time. But, if this?
$$y=x^2,z=y^2$$
This will be more complicated. But if this?
$$z=[f(x)]^2$$
Everything is useful, you think they are useless just because you do not see their benefit to you.
